// Here, I should have the config:

// https://www.chartjs.org/docs/3.2.0/samples/line/line.html

const actions = [{}, {}, {}];

// mulig dette kan gjøre ting litt enklere.
const labels = [];

const options = {};

module.exports = {
  actions: actions,
  options: options,
};
